学習ログ：ProjectInput クラスと入力値取得処理の復習
今日のテーマ

今日は TypeScript のプロジェクト管理アプリ課題で、模範解答の ProjectInput クラスを復習した。

主に以下を確認した。

templateElement / hostElement / element の違い
constructor がいつ動くのか
this.templateElement.content の意味
document.importNode(..., true) の意味
firstElementChild as HTMLFormElement の意味
attach() を別メソッドにしている理由
insertAdjacentElement("afterbegin", this.element) の意味
自作コードと模範解答の対応関係
@autobind を付ける場所
gatherUserInput(): [string, string, number] | void の意味
タプル [string, string, number] の使い方
今日理解したこと
templateElement / hostElement / element の違い
templateElement: HTMLTemplateElement;
hostElement: HTMLDivElement;
element: HTMLFormElement;

それぞれの役割は次の通り。

templateElement = コピー元の <template>
hostElement     = 表示先の <div id="app">
element         = template からコピーして作った <form>

つまり、this.element は HTML に最初から見えているものではなく、template からコピーして作られたフォーム。

<form>
  <div class="form-control">
    <label for="title">タイトル</label>
    <input type="text" id="title" />
  </div>
  ...
</form>

これが this.element に入る。

constructor が動くタイミング

constructor は、クラスを書いたときではなく、new したときに動く。

const prjInput = new ProjectInput();

この瞬間に ProjectInput クラスの constructor() が実行される。

流れは次の通り。

new ProjectInput()
↓
constructor が動く
↓
template を取得
↓
app を取得
↓
template の中身をコピー
↓
form を取り出す
↓
input 要素を取得
↓
configure() で submit イベント登録
↓
attach() で画面に追加
this.templateElement.content の意味
this.templateElement.content

これは <template> タグの中身を指す。

<template> は画面に直接表示されるものではなく、HTML の部品を保管しておく場所。

<template id="project-input">
  <form>
    ...
  </form>
</template>

この場合、

this.templateElement

は <template> 全体。

this.templateElement.content

は <template> の中に入っている DOM のかたまり。

document.importNode(..., true) の意味
const importedNode = document.importNode(
  this.templateElement.content,
  true,
);

これは、template の中身をコピーする処理。

第2引数の true は、子要素も含めて深くコピーするという意味。

true = 中の form / div / label / input なども全部コピーする
firstElementChild as HTMLFormElement の意味
this.element = importedNode.firstElementChild as HTMLFormElement;

importedNode はコピーされた DOM のかたまり。

その中の最初の要素が <form> なので、firstElementChild で取り出している。

ただし TypeScript から見ると、firstElementChild はただの Element | null として扱われる。

なので、

as HTMLFormElement

を付けて、

これは form 要素として扱う

と TypeScript に教えている。

attach() を別メソッドにしている理由
private attach() {
  this.hostElement.insertAdjacentElement("afterbegin", this.element);
}

attach() は、フォームを画面に追加するための内部処理。

constructor に直接書いても動くが、別メソッドにすることで役割がわかりやすくなる。

constructor = 初期設定の流れを書く場所
attach      = DOM にフォームを追加する処理

また、private を付けることで、クラスの外から勝手に呼べないようにしている。

const prjInput = new ProjectInput();

// prjInput.attach(); 
// private なので外からは呼べない
insertAdjacentElement("afterbegin", this.element) の意味
this.hostElement.insertAdjacentElement("afterbegin", this.element);

これは、#app の中の先頭に this.element を追加する処理。

<div id="app">
  <!-- afterbegin はここに入る -->
</div>

実行後は次のようになる。

<div id="app">
  <form id="user-input">
    ...
  </form>
</div>
@autobind を付ける場所

最初は element に付けようとしていたが、それは違った。

@autobind
element: HTMLFormElement;

これは間違い。

@autobind は、イベントリスナーに渡すメソッドに付ける。

正しくは次のように submitHandler に付ける。

@autobind
private submitHandler(event: Event) {
  event.preventDefault();
  const userInput = this.gatherUserInput();

  if (Array.isArray(userInput)) {
    const [title, desc, manday] = userInput;
    console.log(title, desc, manday);
    this.clearInputs();
  }
}

理由は、ここで submitHandler をイベントリスナーに渡しているから。

this.element.addEventListener("submit", this.submitHandler);

このとき this がずれる可能性がある。

@autobind によって、submitHandler の中の this を ProjectInput インスタンスに固定できる。

タプルについて

今日のコードでは、gatherUserInput() の戻り値にタプルを使った。

private gatherUserInput(): [string, string, number] | void

これは、

成功したら [string, string, number] を返す
失敗したら何も返さない

という意味。

具体的には、

return [enteredTitle, enteredDescription, +enteredManday];

この戻り値は、

1番目: title       string
2番目: description string
3番目: manday      number

という形で固定されている。

普通の配列ではなく、順番と型が決まった配列なので、タプル。

今日のコード
// autobind decorator
function autobind(_: any, _2: string, descriptor: PropertyDescriptor) {
  const originalMethod = descriptor.value;

  const adjDescriptor: PropertyDescriptor = {
    configurable: true,
    get() {
      const boundFn = originalMethod.bind(this);
      return boundFn;
    },
  };

  return adjDescriptor;
}

// interface
interface validatorConfig {
  [prop: string]: {
    [validatableProp: string]: string[]; // ["required", "positive"]
  };
}

const registeredValidators: validatorConfig = {};

function Required(target: any, propName: string) {
  registeredValidators[target.constructor.name] = {
    ...registeredValidators[target.constructor.name],
    [propName]: [
      ...(registeredValidators[target.constructor.name]?.[propName] ?? []),
      "required",
    ],
  };
}

function PositiveNumber(target: any, propName: string) {
  registeredValidators[target.constructor.name] = {
    ...registeredValidators[target.constructor.name],
    [propName]: [
      ...(registeredValidators[target.constructor.name]?.[propName] ?? []),
      "positive",
    ],
  };
}

function validate(obj: any) {
  const objValidatorConfig = registeredValidators[obj.constructor.name];

  if (!objValidatorConfig) {
    return true;
  }

  let isValid = true;

  for (const prop in objValidatorConfig) {
    const validators = objValidatorConfig[prop];

    if (!validators) {
      continue;
    }

    for (const validator of validators) {
      switch (validator) {
        case "required":
          isValid = isValid && !!obj[prop];
          break;
        case "positive":
          isValid = isValid && obj[prop] > 0;
          break;
      }
    }
  }

  return isValid;
}

// ProjectInput Class
class ProjectInput {
  templateElement: HTMLTemplateElement;
  hostElement: HTMLDivElement;
  element: HTMLFormElement;
  titleInputElement: HTMLInputElement;
  descriptionInputElement: HTMLTextAreaElement;
  mandayInputElement: HTMLInputElement;

  constructor() {
    this.templateElement = document.getElementById(
      "project-input",
    )! as HTMLTemplateElement;

    this.hostElement = document.getElementById("app")! as HTMLDivElement;

    const importedNode = document.importNode(
      this.templateElement.content,
      true,
    );

    this.element = importedNode.firstElementChild as HTMLFormElement;
    this.element.id = "user-input";

    this.titleInputElement = this.element.querySelector(
      "#title",
    ) as HTMLInputElement;

    this.descriptionInputElement = this.element.querySelector(
      "#description",
    ) as HTMLTextAreaElement;

    this.mandayInputElement = this.element.querySelector(
      "#manday",
    ) as HTMLInputElement;

    this.configure();
    this.attach();
  }

  private gatherUserInput(): [string, string, number] | void {
    const enteredTitle = this.titleInputElement.value;
    const enteredDescription = this.descriptionInputElement.value;
    const enteredManday = this.mandayInputElement.value;

    if (
      validate({ value: enteredTitle, required: true, minLength: 5 }) &&
      validate({ value: enteredDescription, required: true, minLength: 5 }) &&
      validate({ value: enteredManday, required: true, minLength: 5 })
    ) {
      alert("入力値が正しくありません。再度お試しください。");
      return;
    } else {
      return [enteredTitle, enteredDescription, +enteredManday];
    }
  }

  private clearInputs() {
    this.titleInputElement.value = "";
    this.descriptionInputElement.value = "";
    this.mandayInputElement.value = "";
  }

  @autobind
  private submitHandler(event: Event) {
    event.preventDefault();

    const userInput = this.gatherUserInput();

    if (Array.isArray(userInput)) {
      const [title, desc, manday] = userInput;
      console.log(title, desc, manday);
      this.clearInputs();
    }
  }

  private configure() {
    this.element.addEventListener("submit", this.submitHandler);
  }

  private attach() {
    this.hostElement.insertAdjacentElement("afterbegin", this.element);
  }
}

const prjInput = new ProjectInput();
次回やること

次回はこの続きから実施する。

主に講義のやり方を確認する。

特に確認するポイントはここ。

private gatherUserInput(): [string, string, number] | void {
  const enteredTitle = this.titleInputElement.value;
  const enteredDescription = this.descriptionInputElement.value;
  const enteredManday = this.mandayInputElement.value;

  if (
    validate({ value: enteredTitle, required: true, minLength: 5 }) &&
    validate({ value: enteredDescription, required: true, minLength: 5 }) &&
    validate({ value: enteredManday, required: true, minLength: 5 })
  ) {
    alert("入力値が正しくありません。再度お試しください。");
    return;
  } else {
    return [enteredTitle, enteredDescription, +enteredManday];
  }
}

現時点では、前に学習した デコレーター式バリデーション と、講義の Validatable オブジェクト方式 が混ざっている。

次回は講義の模範解答に合わせて、次の形を確認する予定。

interface Validatable {
  value: string | number;
  required?: boolean;
  minLength?: number;
  maxLength?: number;
  min?: number;
  max?: number;
}

そして、validate() もこの Validatable を受け取る形に修正する。

今日のまとめ

今日の一番大事な理解はこれ。

ProjectInput クラスは、
template から form をコピーして、
その form の中の input を取得し、
submit イベントを登録して、
入力値をタプルとして返すためのクラス。

また、@autobind は element ではなく、イベントリスナーに渡す submitHandler に付ける。

@autobind
private submitHandler(event: Event) {
  event.preventDefault();
}

次回は、講義の Validatable 方式に合わせて、validate() と gatherUserInput() の流れを整理する。